export type TData = {
  jogo: number
  data: string
  hora: string
  local: string
  grupo: string
  mandante: string
  placarMandante: number | string
  placarVisitante: number | string
  visitante: string
}
